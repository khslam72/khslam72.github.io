name: Submit Selected URLs to Google Indexing API

on:
  workflow_dispatch:
    inputs:
      mode:
        description: "Choose which posts to index"
        required: true
        type: choice
        options:
          - today
          - yesterday
          - manual
      manual_urls:
        description: "If manual mode: enter one or more URLs separated by spaces"
        required: false
        default: ""

jobs:
  indexing:
    runs-on: ubuntu-latest
    env:
      TZ: "Asia/Seoul"

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v1
        with:
          credentials_json: '${{ secrets.SERVICE_ACCOUNT_KEY }}'
          access_token_scopes: 'https://www.googleapis.com/auth/indexing'

      - name: Determine URLs to index
        id: get_urls
        run: |
          MODE="${{ github.event.inputs.mode }}"
          URLS=""

          if [ "$MODE" == "today" ]; then
            echo "🕒 Mode: today - finding posts modified in the last 1 day"
            FILES=$(find _posts -type f -mtime -1)
            for file in $FILES; do
              FILENAME=$(basename "$file" .md)
              TITLE_PART=$(echo "$FILENAME" | cut -d'-' -f4-)
              URL="https://kcontents.site/${TITLE_PART}/"
              URLS="$URLS $URL"
            done
          elif [ "$MODE" == "yesterday" ]; then
            echo "🕒 Mode: yesterday - finding posts modified 1–2 days ago"
            FILES=$(find _posts -type f -mtime 1)
            for file in $FILES; do
              FILENAME=$(basename "$file" .md)
              TITLE_PART=$(echo "$FILENAME" | cut -d'-' -f4-)
              URL="https://kcontents.site/${TITLE_PART}/"
              URLS="$URLS $URL"
            done
          elif [ "$MODE" == "manual" ]; then
            echo "📝 Manual mode - using input URLs"
            URLS="${{ github.event.inputs.manual_urls }}"
          fi

          echo "URLs to index: $URLS"
          echo "URLS_TO_INDEX=$URLS" >> $GITHUB_ENV

      - name: Submit to Google Indexing API
        if: env.URLS_TO_INDEX != ''
        run: |
          for url_to_index in ${{ env.URLS_TO_INDEX }}; do
            echo "Submitting: $url_to_index"
            response_code=$(curl -s -o /dev/null -w "%{http_code}" -X POST "https://indexing.googleapis.com/v3/urlNotifications:publish" \
              -H "Authorization: Bearer $(gcloud auth print-access-token)" \
              -H "Content-Type: application/json" \
              --data "{ \"url\": \"$url_to_index\", \"type\": \"URL_UPDATED\" }")
            if [ "$response_code" -eq 200 ]; then
              echo "✅ Successfully submitted: $url_to_index"
            else
              echo "❌ Error submitting: $url_to_index (Response code: $response_code)"
            fi
          done
